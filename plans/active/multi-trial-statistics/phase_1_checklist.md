# Phase 1: Core Data Structures and Trial Management Checklist

**Initiative:** Multi-Trial Statistics
**Created:** 2025-01-24
**Phase Goal:** To implement the foundational data structures for multi-trial storage and the core trial management system.
**Deliverable:** A new module `eryx/statistics/trial_manager.py` with data structures for trial storage, ID generation, and basic CRUD operations, along with comprehensive unit tests.

## ✅ Task List

### Instructions:
1.  Work through tasks in order. Dependencies are noted in the guidance column.
2.  The **"How/Why & API Guidance"** column contains all necessary details for implementation.
3.  Update the `State` column as you progress: `[ ]` (Open) -> `[P]` (In Progress) -> `[D]` (Done).

---

| ID  | Task Description                                   | State | How/Why & API Guidance                                                                                                                                                                                                                                                                                                                                                      |
| :-- | :------------------------------------------------- | :---- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Section 0: Project Structure Setup**
| 0.A | **Create statistics module directory**                    | `[ ]` | **Why:** Establish proper module structure for the new statistics functionality. <br> **How:** Create directory `eryx/statistics/` and add `__init__.py` file. <br> **File:** `eryx/statistics/__init__.py` <br> **Verify:** Directory exists and is importable as a Python module.
| 0.B | **Create test directory structure**| `[ ]` | **Why:** Establish proper test structure matching the module layout. <br> **How:** Create `tests/statistics/` directory and add `__init__.py` and `test_trial_manager.py` files. <br> **Files:** `tests/statistics/__init__.py`, `tests/statistics/test_trial_manager.py` <br> **Verify:** Test discovery works with `pytest tests/statistics/`
| **Section 1: Core Data Classes**
| 1.A | **Define Trial data class**                   | `[ ]` | **Why:** Core data structure to represent a single experimental trial. <br> **How:** Create `@dataclass` with fields: `trial_id: str`, `timestamp: datetime`, `parameters: Dict[str, Any]`, `metrics: Dict[str, float]`, `metadata: Dict[str, Any]`, `status: TrialStatus`. Import from `typing`, `datetime`, `dataclasses`, `enum`. <br> **File:** `eryx/statistics/trial_manager.py` <br> **Verify:** Can instantiate Trial objects with all fields.
| 1.B | **Define TrialStatus enumeration**                   | `[ ]` | **Why:** Track trial execution state with type safety. <br> **How:** Create `class TrialStatus(Enum)` with values: `PENDING`, `RUNNING`, `COMPLETED`, `FAILED`, `CANCELLED`. <br> **File:** `eryx/statistics/trial_manager.py` <br> **Verify:** Enum values are accessible and comparable.
| 1.C | **Define TrialCollection data class**                   | `[ ]` | **Why:** Container for managing multiple trials with metadata. <br> **How:** Create `@dataclass` with fields: `collection_id: str`, `name: str`, `description: str`, `created_at: datetime`, `trials: List[Trial]`, `collection_metadata: Dict[str, Any]`. <br> **File:** `eryx/statistics/trial_manager.py` <br> **Verify:** Can create collections and add trials to them.
| 1.D | **Add Trial validation methods**                   | `[ ]` | **Why:** Ensure data integrity and catch errors early. <br> **How:** Add `validate()` method to Trial class that checks: trial_id is non-empty, timestamp is valid datetime, metrics contain only numeric values, status is valid TrialStatus. Raise `ValueError` for invalid data. <br> **File:** `eryx/statistics/trial_manager.py` <br> **Verify:** Validation correctly rejects invalid trials and accepts valid ones.
| **Section 2: Trial ID Generation**
| 2.A | **Implement UUID-based trial ID generator**                   | `[ ]` | **Why:** Ensure unique, collision-resistant trial identifiers. <br> **How:** Create function `generate_trial_id(prefix: str = "trial") -> str` that returns `f"{prefix}_{uuid.uuid4().hex[:8]}"`. Import `uuid`. <br> **File:** `eryx/statistics/trial_manager.py` <br> **Verify:** Generated IDs are unique across multiple calls and follow expected format.
| 2.B | **Add timestamp-based ID generation option**                   | `[ ]` | **Why:** Support human-readable, chronologically sortable IDs. <br> **How:** Add optional parameter `use_timestamp: bool = False` to `generate_trial_id()`. When True, return `f"{prefix}_{datetime.now().strftime('%Y%m%d_%H%M%S')}_{uuid.uuid4().hex[:4]}"`. <br> **File:** `eryx/statistics/trial_manager.py` <br> **Verify:** Timestamp-based IDs are sortable and still unique.
| **Section 3: Storage Backend Interface**
| 3.A | **Define abstract storage backend**                   | `[ ]` | **Why:** Enable pluggable storage backends (memory, file, database). <br> **How:** Create `class TrialStorageBackend(ABC)` with abstract methods: `save_trial(trial: Trial) -> None`, `load_trial(trial_id: str) -> Trial`, `list_trials() -> List[str]`, `delete_trial(trial_id: str) -> None`, `save_collection(collection: TrialCollection) -> None`, `load_collection(collection_id: str) -> TrialCollection`. Import `abc.ABC, abc.abstractmethod`. <br> **File:** `eryx/statistics/trial_manager.py` <br> **Verify:** Interface is properly abstract and cannot be instantiated directly.
| 3.B | **Implement in-memory storage backend**                   | `[ ]` | **Why:** Fast storage for development and testing scenarios. <br> **How:** Create `class InMemoryStorage(TrialStorageBackend)` with `_trials: Dict[str, Trial]` and `_collections: Dict[str, TrialCollection]` attributes. Implement all abstract methods using dictionary operations. Raise `KeyError` for missing items. <br> **File:** `eryx/statistics/trial_manager.py` <br> **Verify:** All CRUD operations work correctly with in-memory storage.
| 3.C | **Implement file-based storage backend**                   | `[ ]` | **Why:** Persistent storage for real experiments. <br> **How:** Create `class FileStorage(TrialStorageBackend)` with `storage_dir: Path` parameter. Use JSON serialization with custom encoder for datetime objects. Store trials as `{storage_dir}/trials/{trial_id}.json` and collections as `{storage_dir}/collections/{collection_id}.json`. Import `pathlib.Path`, `json`. <br> **File:** `eryx/statistics/trial_manager.py` <br> **Verify:** Trials persist across storage object instances.
| **Section 4: Serialization Support**
| 4.A | **Add Trial serialization methods**                   | `[ ]` | **Why:** Enable checkpointing and persistence of trial data. <br> **How:** Add `to_dict(self) -> Dict[str, Any]` and `@classmethod from_dict(cls, data: Dict[str, Any]) -> Trial` methods to Trial class. Handle datetime serialization using `.isoformat()` and `datetime.fromisoformat()`. <br> **File:** `eryx/statistics/trial_manager.py` <br> **Verify:** Round-trip serialization preserves all trial data exactly.
| 4.B | **Add TrialCollection serialization methods**                   | `[ ]` | **Why:** Enable collection-level persistence and sharing. <br> **How:** Add `to_dict(self) -> Dict[str, Any]` and `@classmethod from_dict(cls, data: Dict[str, Any]) -> TrialCollection` methods. Serialize nested trials using their `to_dict()` methods. <br> **File:** `eryx/statistics/trial_manager.py` <br> **Verify:** Collections with multiple trials serialize and deserialize correctly.
| 4.C | **Implement JSON encoder for custom types**                   | `[ ]` | **Why:** Handle datetime, Enum, and other non-JSON-native types. <br> **How:** Create `class TrialJSONEncoder(json.JSONEncoder)` that handles `datetime` (using `.isoformat()`), `TrialStatus` (using `.value`), and `Enum` types generally. <br> **File:** `eryx/statistics/trial_manager.py` <br> **Verify:** All trial data types can be JSON serialized without errors.
| **Section 5: Trial Manager Class**
| 5.A | **Implement TrialManager core class**                   | `[ ]` | **Why:** Provide high-level interface for trial management operations. <br> **How:** Create `class TrialManager` with `__init__(self, storage_backend: TrialStorageBackend)` constructor. Add methods: `create_trial(parameters: Dict, metadata: Dict = None) -> Trial`, `get_trial(trial_id: str) -> Trial`, `update_trial_metrics(trial_id: str, metrics: Dict[str, float]) -> None`, `update_trial_status(trial_id: str, status: TrialStatus) -> None`. <br> **File:** `eryx/statistics/trial_manager.py` <br> **Verify:** Can perform all basic trial operations through manager interface.
| 5.B | **Add collection management methods**                   | `[ ]` | **Why:** Support grouping related trials into collections. <br> **How:** Add methods to TrialManager: `create_collection(name: str, description: str = "") -> TrialCollection`, `add_trial_to_collection(trial_id: str, collection_id: str) -> None`, `get_collection(collection_id: str) -> TrialCollection`, `list_collections() -> List[str]`. <br> **File:** `eryx/statistics/trial_manager.py` <br> **Verify:** Collections can be created, populated with trials, and retrieved.
| 5.C | **Add trial querying and filtering**                   | `[ ]` | **Why:** Enable efficient access to subsets of trials. <br> **How:** Add methods: `find_trials(status: TrialStatus = None, parameters: Dict = None) -> List[Trial]`, `get_trials_by_status(status: TrialStatus) -> List[Trial]`, `get_recent_trials(limit: int = 10) -> List[Trial]`. Use list comprehensions with filtering logic. <br> **File:** `eryx/statistics/trial_manager.py` <br> **Verify:** Filtering returns correct subsets of trials based on criteria.
| **Section 6: Unit Tests Implementation**
| 6.A | **Test Trial data class functionality**                   | `[ ]` | **Why:** Verify core data structures work as expected. <br> **How:** In `test_trial_manager.py`, create test functions: `test_trial_creation()`, `test_trial_validation()`, `test_trial_serialization()`. Test valid/invalid inputs, edge cases, and error conditions. Use `pytest.raises()` for exception testing. <br> **File:** `tests/statistics/test_trial_manager.py` <br> **Verify:** All Trial functionality is covered with >90% code coverage.
| 6.B | **Test storage backend implementations**                   | `[ ]` | **Why:** Ensure all storage backends work correctly and consistently. <br> **How:** Create parameterized tests using `@pytest.mark.parametrize` with both InMemoryStorage and FileStorage backends. Test all CRUD operations, error handling, and edge cases. Use temporary directories for file tests. <br> **File:** `tests/statistics/test_trial_manager.py` <br> **Verify:** Both storage backends pass identical test suites.
| 6.C | **Test TrialManager integration**                   | `[ ]` | **Why:** Verify the high-level interface works correctly. <br> **How:** Create tests for all TrialManager methods: trial creation, status updates, collection management, querying. Test both success and failure scenarios. Mock storage backend where needed for isolation. <br> **File:** `tests/statistics/test_trial_manager.py` <br> **Verify:** TrialManager provides reliable high-level interface to all functionality.
| 6.D | **Test edge cases and error handling**                   | `[ ]` | **Why:** Ensure robust behavior under unusual conditions. <br> **How:** Test scenarios: empty collections, non-existent trial IDs, corrupted data files, invalid parameters, concurrent access patterns. Verify appropriate exceptions are raised with helpful messages. <br> **File:** `tests/statistics/test_trial_manager.py` <br> **Verify:** System handles edge cases gracefully without crashes.
| **Section 7: Module Integration and Documentation**
| 7.A | **Update statistics module __init__.py**                   | `[ ]` | **Why:** Make key classes easily importable. <br> **How:** In `eryx/statistics/__init__.py`, add imports: `from .trial_manager import Trial, TrialCollection, TrialStatus, TrialManager, InMemoryStorage, FileStorage`. <br> **File:** `eryx/statistics/__init__.py` <br> **Verify:** Can import all classes with `from eryx.statistics import Trial, TrialManager`.
| 7.B | **Add docstrings to all public classes and methods**                   | `[ ]` | **Why:** Provide clear API documentation for future users. <br> **How:** Add comprehensive docstrings following Google style to all public classes, methods, and functions. Include parameter descriptions, return types, and usage examples. <br> **File:** `eryx/statistics/trial_manager.py` <br> **Verify:** All public APIs have clear, helpful documentation.
| 7.C | **Run final tests and check coverage**                   | `[ ]` | **Why:** Ensure all functionality is tested and working. <br> **How:** Run `pytest tests/statistics/test_trial_manager.py -v --cov=eryx.statistics --cov-report=term-missing`. Verify 100% pass rate and >90% code coverage. Fix any failing tests or coverage gaps. <br> **File:** Command line <br> **Verify:** Tests pass with high coverage as specified in success criteria.

---

## 🎯 Success Criteria

**This phase is complete when:**
1.  All tasks in the table above are marked `[D]` (Done).
2.  The phase success test passes: `pytest tests/statistics/test_trial_manager.py` completes with 100% pass rate and >90% code coverage.
3.  No regressions are introduced in the existing test suite.