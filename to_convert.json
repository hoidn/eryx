{
  "models.py": {
    "class onephonon": {
      "__init__": "constructor that initializes the onephonon model with pdb path, sampling parameters, and gnm parameters. must convert all core data structures to pytorch tensors for differentiation.",
      "_setup": "core setup function that initializes reciprocal space grid, resolution mask, and sets up the crystal object. initializes tensors for computations.",
      "_setup_phonons": "sets up phonon calculations: initializes k-vector grid, dynamical matrices, and eigenvalue arrays as tensors.",
      "_build_a": "builds the displacement projection matrix (a matrix) that projects rigid-body displacements to individual atomic displacements.",
      "_build_m": "builds the mass matrix for the normal mode analysis, projects the all-atom mass matrix using the a matrix.",
      "_build_m_allatoms": "creates the atomic mass matrix from element weights. this is diagonal with atomic masses.",
      "_project_m": "projects the all-atom mass matrix to rigid-body degrees of freedom using a matrix projections.",
      "_build_kvec_brillouin": "computes all k-vectors and their norms in the first brillouin zone by regularly sampling reciprocal space.",
      "_center_kvec": "helper function for k-vector centering within brillouin zone. applies periodic boundary conditions.",
      "_at_kvec_from_miller_points": "maps miller indices to reciprocal space indices. used to find indices of all q-vectors that are k-vector away from miller indices.",
      "compute_gnm_phonons": "computes phonon modes from the gaussian network model (gnm) at each k-vector in the brillouin zone.",
      "compute_hessian": "builds the hessian matrix for the elastic network model of the crystal, capturing interatomic force constants.",
      "compute_covariance_matrix": "computes the covariance matrix for atomic displacements, using the gnm hessian and scaling to match experimental adps.",
      "apply_disorder": "main computation function that produces diffuse intensity by applying the one-phonon approximation to the structure factors."
    }
  },
  "scatter.py": {
    "compute_form_factors": "evaluates atomic form factors at given q-vectors using the parameterized form factor coefficients.",
    "structure_factors_batch": "computes structure factors for a batch of q-vectors, including atomic form factors, phase factors, and thermal displacement factors.",
    "structure_factors": "wrapper that breaks calculation into batches and optionally parallelizes the computation of structure factors."
  },
  "pdb.py": {
    "class gaussiannetworkmodel": {
      "__init__": "constructor for gnm that sets up atomic interactions with spring constants.",
      "_setup_atomic_model": "builds unit cell and nearest neighbors while storing dimensions needed for computation.",
      "_setup_gaussian_network_model": "builds interaction pair lists and spring constants for the elastic network.",
      "build_gamma": "creates tensor of spring constants between atom pairs, with different values for intra-asu vs. inter-asu interactions.",
      "build_neighbor_list": "uses spatial tree search to identify atom pairs within cutoff distance for elastic interactions.",
      "compute_hessian": "builds the hessian matrix from interaction pairs and spring constants, encoding force relationships.",
      "compute_k": "computes the dynamical matrix for a given k-vector by summing hessian blocks with phase factors.",
      "compute_kinv": "computes the inverse of the dynamical matrix, which relates to atomic displacement correlations."
    },
    "class atomicmodel": {
      "_get_xyz_asus": "transforms atomic coordinates using symmetry operations to generate all asymmetric units in the unit cell.",
      "flatten_model": "reshapes multi-dimensional tensor arrays to simplify computations."
    },
    "class crystal": {
      "get_asu_xyz": "returns atomic coordinates for a specific asymmetric unit in a specified unit cell, used in diffuse scattering calculations."
    }
  },
  "map_utils.py": {
    "generate_grid": "creates a grid of q-vectors (or hkl indices) based on desired extents and spacing in reciprocal space.",
    "get_resolution_mask": "creates boolean mask indicating which grid points are within resolution limits.",
    "compute_resolution": "calculates the resolution in angstroms for given miller indices based on unit cell parameters."
  }
}
